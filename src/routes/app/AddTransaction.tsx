import { RouteComponentProps } from "@reach/router";
import React from "react";

const AddTransaction = (props: RouteComponentProps) => {
  return <div>AddTransaction</div>;
};

export default AddTransaction;
